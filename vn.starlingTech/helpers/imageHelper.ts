// import {isEmpty} from 'lodash';

// const getImgSource = (imgUrl: string, hostUrl?: string) => {
//   if (isEmpty(imgUrl)) {
//     return null;
//   }
//   return imgUrl.includes('http') ? {uri: imgUrl} : {uri: `${hostUrl}${imgUrl}`};
// };
